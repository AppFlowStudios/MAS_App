import { View } from 'react-native'
import React from 'react'
type SheikPopoverProp = {
  visible : boolean,
  anchor : Element,
  renderToggleButton  : Function
}
const SheikPopover = ({visible, anchor} : SheikPopoverProp) => {
  return (
    <View></View>
  )
}

export default SheikPopover